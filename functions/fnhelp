# vi: ft=zsh
##? fnhelp - use '##?' comments in function files as help docs.
##?
##? usage: fnhelp <func>

0=${(%):-%x}
local funcdir="${0:h}"
if [[ ! -f "$funcdir/$1" ]]; then
  echo >&2 "fnhelp: function not found '$1'." && return 1
fi
command grep "^##?" "$funcdir/$1" | cut -c 5-

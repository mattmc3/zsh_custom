# function omzplugin_exists() {
#   test -d "$ZSH_CUSTOM"/external/$1
# }

# function omzplugin_install() {
#   if [[ ! -d "$ZSH_CUSTOM"/external/github.com/zsh-users/zsh-autosuggestions ]]; then
#     mkdir -p "$ZSH_CUSTOM"/external/github.com/zsh-users
#     git clone --depth 1
#   fi
# }
